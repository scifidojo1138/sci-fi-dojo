# Sci-Fi Dojo — Project Handoff (Technical)

Last updated: 2026-07-06. Update this file whenever architecture, endpoints, or conventions change.

## Project Overview

Sci-Fi Dojo (SFD) is a membership-based physical media rental club operating in New Jersey. Members rent Blu-ray, DVD, and 4K UHD discs from a self-service cabinet. The tech stack is intentionally minimal — no framework, no database, no server — just single-file HTML/CSS/JS apps backed by Google Apps Script and Google Sheets.

## Architecture

### Frontend
- **Single-file HTML/CSS/JS apps** — no build step, no bundler, no framework. Vanilla JS only.
- Fonts: Orbitron, Share Tech Mono, Exo 2 (Google Fonts, loaded via `<link>` + preconnect — never CSS `@import`, which blocks rendering)
- Dark blue-cyan palette with CSS variables (see `:root` block in each file)
- Deployed via **GitHub → Netlify** (committing to the repo IS the deploy)
- Domain: **scifidojo.com** (DNS via Cloudflare)

### Apps — two repos

**This repo (`sci-fi-dojo`, public):**
| File | URL | Description |
|---|---|---|
| `member.html` | `scifidojo.com/member?token=tok_xxx` | Member app (legacy model, kept until cutover) |
| `rent.html` | `scifidojo.com/rent?token=cus_xxx` | Pay-per-rental customer app (Redbox model; no token = signup). Presented publicly as a free SFD Account — see "Pay-Per-Rental" below. |
| `terms.html` | `scifidojo.com/terms` | Rental terms |
| `BACKEND-UPDATE.md` | — | Apps Script change log / paste instructions |
| `TERMS-CHECKLIST.md` | — | Clause checklist for revising `/terms` around pay-per-rental billing |

There is no `index.html`; `netlify.toml` redirects `/` to the Instagram profile (`@scifi_dojo`) until a proper landing page exists.

**Onboarding repo (private Netlify site: `scifidojo-onboarding.netlify.app`):**
| File | Description |
|---|---|
| `sfd-onboarding.html` | Staff onboarding terminal (tier selection, Stripe checkout) |
| `success.html` | Post-payment enrollment confirmation + QR card |
| `sfd-staff.html` | Staff terminal (shares the onboarding terminal's teal/cyan style since 2026-07-08): attention strip, cabinet codes, outstanding rentals + sweep, customer search + card printing; legacy member tools collapsed at the bottom |
| `cards/print-cards.html` | Member card print layout |
| Netlify functions | `create-checkout`, `onboard-member`, `process-return`, `start-rental`, `rental-webhook`, `charge-rental`, `billing-sweep` |

### Backend
- **Google Apps Script** web app connected to **SciFiDojo_Sheet_v2** (Google Sheets)
- Deployed as: Execute as Me, Anyone can access
- Returns JSON. GET with `?action=` param or POST with JSON body
- Current URL: `https://script.google.com/macros/s/AKfycby7PvoGckOSEuE_qBR0Rl12sudFnjnf5rT4rNu0kUSJ_lmFVvAbjBBo5DdlVfNZpnwjDQ/exec`
- **Deployment rule (learned the hard way):** the URL only changes if you create a NEW deployment. To ship backend changes without breaking every client, always publish a new version of the EXISTING deployment: **Deploy → Manage deployments → ✏️ → Version: New version → Deploy.** Saving code in the editor does NOT deploy it.
- A copy of the backend script is kept in the onboarding repo as backup. Committing it does not deploy it.

## Security Model

Two credentials, both checked server-side in Apps Script:

1. **`API_KEY`** — shared constant defined in both the Apps Script and each client page. Sent as `&key=` on GETs, `api_key` in POST bodies. Gates the member-facing actions: `member`, `catalog`, `checkout`, `return_log`, `member_request`. The key is visible in page source by design — it deters bots and casual URL abuse. Real per-member security is the membership token. To rotate: change it in the Apps Script AND every client page, then redeploy both.
2. **`staff_pin`** — lives only in the Settings tab of the sheet (never in any page source). Sent as `&pin=` on GETs. Gates the staff actions: `all_members`, `active_checkouts`, `return_bin`, `catalog_staff`, and (as `staff_pin` in POST body) `return_processed`, `correction`, `rental_close`, `process_kept_return`, `reactivate_customer`, `customer_card_printed`. The staff page's login gate verifies the typed PIN against the backend — there is no hardcoded staff code in `sfd-staff.html`. **Rate-limited as of 2026-07-14:** every check now goes through `checkStaffPin_()` instead of a direct string compare — after `staff_pin_max_fails` wrong attempts (Settings, default 8) within a rolling 5-minute window, ALL pin checks refuse (even a correct one) for `staff_pin_lockout_minutes` (Settings, default 15). The lockout is global via CacheService, not per-caller, since Apps Script has no reliable way to see a caller's IP; a staff member fat-fingering their PIN a couple of times never hits it, a script grinding through guesses does almost immediately.

3. **`SERVER_KEY`** — server-to-server secret shared only between the Apps Script and the Netlify functions (env `SFD_SERVER_KEY`; never in any page source). Gates the payment-recording rental actions: `rent_confirm`, `rent_charge_recorded`, `rent_charge_lookup`, `rental_payment_failed`, `rent_card_lookup`, `rent_card_updated`, and — **as of 2026-07-14** — `onboard` and `update_stripe`. Without it, anyone with the public API key could mark rentals as paid or self-activate a member account.

**`onboard`/`update_stripe` hardening (2026-07-14):** the gate is now live (previously commented out) and, critically, uses `SERVER_KEY` rather than `API_KEY` — the right call, since `API_KEY` is deliberately visible in every page's source and gating on it would have let any member self-activate their own account by POSTing directly to this endpoint. `doOnboard` also no longer trusts the caller's `active_status`/`vault_access` at all: it always writes `pending`/`unpaid` and derives `vault_access` from a server-side `TIER_VAULT_ACCESS` tier map (Premiere/Donor → true, matching the Membership Tiers table — keep the two in sync if tiers ever change).

⚠️ **Action needed:** this means the onboarding repo's `onboard-member.js` and `stripe-webhook.js` Netlify functions must send `server_key: process.env.SFD_SERVER_KEY` in their POST body (not `api_key` — an earlier round of hardening added `api_key` to those two functions, which will now fail this gate and break member signup/Stripe activation entirely once this backend is deployed). Verify/fix those two files before deploying this version. `SFD_SERVER_KEY` is already a Netlify env var (the other four rental functions already use it), so no new env var is needed — just the field name in the POST body.

The membership token rides in the member URL (`?token=`) by QR-card design; `member.html` carries `<meta name="robots" content="noindex, nofollow">` so token links never get search-indexed.

## Apps Script — Endpoints

### GET
- `?action=member&key=&token=` — full member object (rentals, return_pending, rental_history, cabinet/vault codes, `location`, `active_promo`). `tier_limit`/`loan_days` already include any active promo bonus.
- `?action=catalog&key=&token=` — filtered catalog (in_rotation=TRUE only; vault items only with vault_access; filtering is backend-side, never client-side)
- `?action=events&key=&token=` — active, non-past events (Events tab); past events auto-hide by date so staff need not toggle them off
- `?action=perks&key=&token=` — active perks (Perks tab)
- `?action=updates&key=&token=` — active updates ("The Weekly Rewind"), newest first (Updates tab)
- `?action=ping` — uptime check (added 2026-07-14): no key, no sheet reads, instant `{ok:true, ts}` response. Cheap enough to poll constantly, and it doubles as confirmation the deployment itself (not just Sheets access) is alive. **Monitored via a relay, not directly (2026-07-17):** UptimeRobot's free-plan HTTP monitors send HEAD requests, which Google rejects with a 403 before any Apps Script runs (web apps only implement GET/POST) — so the live UptimeRobot monitor points at the onboarding repo's `ping.js` Netlify function (`scifidojo-onboarding.netlify.app/.netlify/functions/ping`), which accepts HEAD, GETs this action itself, and returns 200 only when the response is genuinely `{ok:true}` (a deployment serving an error page with a 200 still shows as DOWN — keyword-level checking on the free plan). A second monitor watches `scifidojo.com/rent` directly (Netlify handles HEAD fine).
- `?action=all_members&pin=` — staff: full member list
- `?action=active_checkouts&pin=` / `?action=return_bin&pin=` / `?action=catalog_staff&pin=` — staff lists. `catalog_staff` takes an optional `&all=true` (added 2026-07-15 for the sister repo's label-printer feature): default behavior is unchanged (in-rotation items only), `all=true` returns every catalog row regardless of `in_rotation` so out-of-rotation items can be pre-labeled too. Every returned item now also carries its own `in_rotation` boolean so the printer UI can tell them apart.

### POST (JSON body)
- `checkout` — checks out items, updates catalog status, logs transaction (key)
- `return_log` — member logs return, slot frees immediately, item → return_pending (key)
- `member_request` — notify-me / star / title_request / review / flag / rsvp / rsvp_cancel from the member app; all append to the Requests tab with `request_type` (key). Note: review and flag land in **Requests** (triage queue), not Transactions. `rsvp`/`rsvp_cancel` carry the event_id in `item_id` and the event title in `text` (gives staff a headcount).
- `return_processed` — staff confirms physical return (staff_pin)
- `correction` — admin note (staff_pin)
- `star` — legacy route, superseded by `member_request`; kept for compatibility
- `damage_reported` — legacy member disc-issue flag (token-validated)
- `onboard` / `update_stripe` — called by Netlify functions during enrollment
- `client_error` — rent.html's error reporting (key; added 2026-07-14, see rent.html notes below)

## Google Sheets — SciFiDojo_Sheet_v2

Apps Script reads columns by header name via `sheetToObjects()`. Tabs:

- **Members:** `member_id, member_token, display_name, stripe_email, contact_email, stripe_phone, contact_phone, tier, active_status, deposit_status, vault_access, invite_status, cabinet_code, next_billing_date, joined_date, notes, stripe_customer_id, stripe_subscription_id, stripe_payment_intent_id, deposit_collected_date`
- **Catalog:** `item_id, title, sort_title, year, edition, format, status, collection, subcategory, group_name, disc_count, condition, acquisition_type, cost, source, notes, in_rotation, last_checked, purchase_price, replacement_cost, replaceability, current_holder_member_id, due_date, rental_price, synopsis, barcode, imdb_id, genre`
  - `sort_title` — populated only when title sorts wrong ("The Matrix" → "Matrix, The"); frontend falls back to `title` when blank
  - `in_rotation` — only TRUE items are served to clients
  - `collection` — "general" or "vault"; `status` — "available" / "checked_out" / "return_pending"
  - `synopsis` (added 2026-07-18) — blank by default, staff-curated, shown in Browse's expanded detail (hide-if-blank, same convention as `rental_price`/`cap_cents`). Usually an actual plot synopsis, but it's just a free-text cell — staff can paste a customer's Leave a Review text into it instead if they want to feature a quote. Either way, this manual copy is the *only* path a submitted review can ever reach a public-facing screen; the review itself still lands privately in Requests (`pending`, staff-only) regardless of whether anyone ever copies it here.
  - `barcode` / `imdb_id` (added 2026-07-21) — internal reference fields only (12-digit UPC, IMDB tt-code). `mapSheetItem` reads them, but `getCatalog`'s `clientFields` deliberately omits both — they never reach rent.html regardless of what the internal item object carries.
  - `genre` (added 2026-07-21) — comma-separated free text (e.g. "Action, Horror, Science-Fiction"), populated for 636 of 689 titles as of the 2026-07-21 sheet; the 53 blanks are mostly multi-movie box sets. `getCatalog` parses it into a trimmed array for the client (blank cell → `[]`, never `['']`) — see Browse below.
- **Catalog Backup:** the REAL library (~759 items). **Catalog is currently demo data (45 items).** See go-live checklist below before swapping.
- **Transactions:** `transaction_id, timestamp, member_id, item_id, action, status_result, note_type, note_text, actor_type, override_type`. Checkout due date is stored in `note_text`.
- **Settings:** decorative header rows — real headers in row 3. Keys: `current_general_code`, `current_vault_code`, `code_rotation_date`, `staff_pin`, `location_address`, `location_maps_url`, `location_hours`. The three `location_*` keys feed the member app's Hours & Location screen; edit them to change address/directions/hours with no redeploy. `location_hours` is a multi-line cell, one "Day<tab>hours" per line (any order — the app sorts Mon→Sun and highlights today).
- **Requests:** `request_id, timestamp, member_id, item_id, request_type, title_text, status, staff_notes`
- **Events:** `event_id, title, date_start, date_end, location, description, rsvp_enabled, image_url, active` — served by `events`; only `active=TRUE` and start date >= today are returned. `date_start`/`date_end` parse as dates (used for the Add to Google Calendar link); `rsvp_enabled` FALSE hides the RSVP button; `image_url` is a root-relative path to a repo-committed image (e.g. `/events/movie-night.jpg`, see `/events/README.md`), shown in the expanded card; blank = no image.
- **Perks:** `perk_id, title, description, image_url, active` — served by `perks`; only `active=TRUE` returned. `image_url` is a root-relative path to a repo-committed image (e.g. `/perks/popcorn.jpg`, see `/perks/README.md`); blank = text-only perk.
- **Updates:** `update_id, date, title, body, active, image_url` — served by `updates` ("The Weekly Rewind"); only `active=TRUE`, newest first by `date`. `body` is freeform; line breaks render in the app. `image_url` (added 2026-07-14) is a root-relative path to a repo-committed image (e.g. `/updates/new-arrivals.jpg`, see `/updates/README.md`), shown at the top of the post when expanded; blank = text-only post, same convention as Events/Perks. The newest unseen update shows a "new" dot per device (localStorage `sfd_rewind_seen_<member_id>`).
- **Promos:** `promo_id, title, description, date_start, date_end, bonus_rentals, bonus_loan_days, tiers, active` — read server-side by `getActivePromo()`. A promo is live when `active=TRUE` and today is within `[date_start, date_end]` (inclusive, Eastern — actually enforced in code as of 2026-07-17 via `todayLocal_()`; it previously compared against the UTC day, which rolls over at 8pm ET, ending promos a few hours early; same fix applies to Rental Promos and Events) and `tiers` is blank (all tiers) or lists the member's tier. Live bonuses are added to the member's **effective** `tier_limit`/`loan_days` in `lookupMember`, so checkout enforcement and availability "just work". The member's permanent plan is also returned as `base_tier_limit`/`base_loan_days`: the app shows **base** on the plan-identity lines (tier line, Membership screen) so a promo ending never looks like a downgrade, while the "Available X of Y" card uses the effective limit and tags the extra with "+N promo". `active_promo` (title + `description` + bonuses + `date_end`) drives the dashboard banner — the banner copy is the staff-written **description**, not auto-generated. A promo only counts as live when at least one bonus is non-zero. Extended loans persist (due dates are stamped per rental); `slotsLeft` clamps at 0 so an over-limit member after a promo ends simply can't check out new items.
- **Dashboard / Expenses:** human-facing, not read by backend logic

## Pay-Per-Rental (Redbox model) — the primary business model

Replaces deposits + memberships (member.html continues to work until cutover). One static QR on the cabinet → `scifidojo.com/rent` → signup (name, email/phone, terms checkbox) → personal `cus_` token URL, bookmarked and reused.

**Framing note:** this is presented to the public as a **free SFD Account** — not a membership. Signup copy says "Create Your SFD Account," the button says "Create Account," and staff print a physical **SFD card** (not a "membership card") using the existing card-print flow (see below). "Membership" is deliberately reserved for a possible future paid tier built on the legacy `Members` tab/tier system (`member.html`); Phase 1 is explicitly an account, with no monthly fee and no deposit, so the free/paid distinction stays legible in conversation even though both are colloquially "signing someone up." Internal code (`Customers` tab, `customer_id`, `customer_token`) already used this language and is unchanged.

### Pricing & accrual (single source of truth: `computeAccrued_` in the Apps Script)
- Base price = Catalog `rental_price` column in dollars (blank defaults to **$3**; $2 remains available as an explicit per-item floor for cheaper titles), covers days 1–7. The 7-day included window matches the shop's Thu–Sun open days: 7 days guarantees every rental spans at least one full open window to return in, no matter which open day it started on (the original 3-day window didn't survive the Mon–Wed closure — a rental could come due while the shop was closed with no way to return it).
- Then $2/day (`RENT_DAILY_CENTS`, bumped from $1 alongside the window extending from 3 to 7 days, to keep per-rental revenue roughly steady), `daysOut = ceil((until - start)/1day)` where `until` = `return_date` if returned else now — so charges freeze the moment the customer logs a return, regardless of when staff process it.
- **24-hour grace (`RENT_GRACE_DAYS = 1`, unadvertised):** extended fees only start once a disc is MORE than one day past the included window (day 9 under 7+1), so a return sitting unseen in the bin overnight never costs a day. The app still says "7 days" everywhere — the grace is a quiet buffer, not a longer window. Trusted-account "on time" tracks the same boundary (`<= RENT_INCLUDED_DAYS + RENT_GRACE_DAYS`).
- **Rental Promos** (pricing promos for pay-per-rental; separate from the legacy member Promos tab): tab `Rental Promos` — `promo_id, title, description, date_start, date_end, daily_rate, base_discount, active`. Live when `active=TRUE`, today within `[date_start, date_end]` (inclusive), and at least one lever set: `daily_rate` replaces the $2 extended-day rate, `base_discount` comes off every base price (floored at $1). First live row wins — pricing promos do not stack. **A rental keeps the deal it started under:** `doRentStart` stamps the discounted base into `base_price` and the promo rate into the Rentals `daily_rate` column (blank = standard rate), and `computeAccrued_` reads the stamped rate, so a promo ending never changes what an open rental accrues. The customer payload carries `active_promo` (title/description/date_end/daily_cents/base_discount_cents/`standard_daily_cents`, the last added 2026-07-14 so the app never has to guess or duplicate `RENT_DAILY_CENTS`) which drives the app's promo banner (staff-written description) and its quoted day rate for new rentals; `getCatalog` sends effective (discounted) prices while a base-discount promo is live, plus `rental_price_was` (the resolved pre-discount price, added 2026-07-14) so the app can show a "was $X, now $Y" strikethrough instead of just silently showing the lower number; `rent_start` returns `daily_cents` so the Stripe Checkout description quotes the promo rate.
- Total charges cap at Catalog `replacement_cost` (blank → Settings `default_payoff_cost`, default $10 — this is a replacement-value ceiling, not a "rate," so it did **not** move with the day-window/price bump above). **Hitting the cap means the disc is kept by default** (decision 2026-07-10, reversing the 2026-07-06 "always expected back" stance): the moment a sweep/manual charge pushes `paid_cents` to the cap on a rental that has NOT been physically returned (`close !== true`), `doRentChargeRecorded` auto-closes it as `paid_off` (not `closed`), pulls the catalog item from rotation (`status: 'sold', in_rotation: 'FALSE'`, `current_holder_member_id` left populated so staff can see who has it), and auto-pauses the customer's account via `maybeAutoPauseForPayoff_()` (see Kept Discs below). A rental that IS being physically checked in (`close === true`) always closes as a normal `closed` regardless of the math — hitting the cap exactly on a real return is still just a return. Avoid the words "late fee" and "replacement cost" in anything customer- or staff-facing; the accrual after the included window is an **extended rental fee** (say it exactly that way — not "$2 a day," not "daily fee" — everywhere the rate is introduced: the Rent screen, item-confirm card, success receipt, Account screen, Browse, terms.html, and the Stripe Checkout description in `start-rental.js`), and the cap is a "maximum charge."
- **Existing catalog rows with an explicit `rental_price` typed in do not move automatically** when these defaults change — only rows left blank pick up the new default. Bumping already-priced titles is a manual (or separately-scripted) sheet edit, not something the backend does for you.

### Kept Discs (rentals that hit the maximum charge)
- A `paid_off` rental means the customer kept the disc by default — no obligation to return it, no deadline if they change their mind. `getRentalBilling()` includes `paid_off` rows alongside the normal active/return_pending ones (tagged by `status`, `due_for_sweep` always false for them) so the staff terminal's **Kept Discs** section reads from the same one call; each row carries `customer_email`/`customer_phone`/`customer_status` for the UI.
- **Auto-pause:** `maybeAutoPauseForPayoff_()` sets the customer's `status` to `paused` the moment a disc is kept (never overwrites an existing `flagged` blacklist status). Paused blocks new rentals (`doRentStart` already rejects any non-`active` status) and hides the cabinet code, exactly like any other inactive account — the rent.html banner text is already generic ("Account paused...") so no app-side change was needed. It's not a penalty, just a hold until staff follow up — the plan is mainly by email.
- **Staff terminal → Kept Discs:** each row shows what was paid, an **EMAIL CUSTOMER** `mailto:` link pre-addressed to the customer with a check-in template, a **REACTIVATE ACCOUNT** button (only shown while paused — POSTs `reactivate_customer`, staff_pin-gated, purely manual per the user's choice, no auto-reactivate), and **PROCESS RETURN** for the rare walk-in where the disc comes back.
- **Walk-in return (`process_kept_return`, staff_pin-gated):** staff decide case-by-case whether to accept the disc back at all (may decline if it's already been replaced or isn't needed) and, if accepted, enter a refund amount (capped server-side at what was actually paid on that rental — never more), an optional note, and whether the item goes back into rotation. This closes the rental for good (`status: 'closed'`) and updates the catalog (`in_rotation` per the staff choice, holder/due_date cleared). **The actual Stripe refund is issued by hand in the Stripe dashboard** — this action only records the decision for bookkeeping; it never calls Stripe. Reactivating the account (above) is a fully independent decision from processing the disc's return — either can happen first, or alone.
- Trusted Accounts needs no special-casing for any of this: `countOnTimeReturns_()` already only counts `closed` rentals within the included window + grace, so `paid_off` rentals (and the eventual `closed` rental from a walk-in return, which by definition ran far past the window to reach the cap) never count toward or against the on-time total.

### Trusted Accounts
- An account's `rental_limit` is raised automatically once it has enough **on-time returns** — closed rentals returned within the included window (`ceil(daysOut) <= RENT_INCLUDED_DAYS`, currently 7 days), computed fresh from Rentals each time by `countOnTimeReturns_()`. Rentals kept past the included window, paid off outright, or still open don't count toward or against the total. Because this reads the same `RENT_INCLUDED_DAYS` constant as the accrual math, "on time" always tracks whatever the included window currently is — it moved from 3 to 7 days for free when the window changed.
- Thresholds live in Settings so they're tunable without a redeploy: `trusted_on_time_threshold` (default 10) and `trusted_rental_limit` (default 2). `maybeUpgradeToTrusted_()` runs after any rental closes (`doRentChargeRecorded` and `doRentalClose`), only raises (never lowers) the limit, and leaves a dated note on the customer's `notes` field so staff can see why it changed.
- Anything beyond this — a customer who's great but occasionally keeps a disc past the included window, wanting a limit above the trusted default, badges/perks/streak mechanics — stays a manual `rental_limit` edit or a deliberately deferred feature; the auto-upgrade only ever does the one thing above.

### Promo Codes (generic marketing codes — free base rental, not per-title)
- Built to "give out and promote the business" — a single memorable, on-theme code (e.g. `usetheforce`) said aloud, printed on a flyer, or posted on social media, not a scannable single-use QR. **Not per-title and not single-use at the code level**: the same code works for any number of different customers. The scarce thing is one **lifetime redemption per customer**, not per code.
- `Promo Codes` tab: `code, active, notes`. Deliberately NOT the same tab as Rental Promos — the two don't actually fit together: Rental Promos floors its base discount at $1 (`Math.max(100, ...)`) and can never reach $0; it's automatic/store-wide (drives the public promo banner for everyone, no code needed) rather than code-gated; and it assumes exactly one active promo at a time ("first live row wins, no stacking"), which would fight with a welcome code that should coexist with a store-wide sale rather than replace it.
- **Redemption** (`redeem_promo`, api_key): looks up the customer by token, checks `promo_redeemed_date` is blank (one lifetime redemption — reusing the same or a different code both get rejected once that's set), matches the typed code case/whitespace-insensitively against an `active=TRUE` row (`norm()` strips everything but a-z0-9, so `"UseTheForce"`/`"use the force"` both hit `usetheforce`), then stamps `promo_redeemed_date` and increments `free_rental_credits` by 1. No `LockService` needed here — unlike `nextCustomerId_()`, there's no shared scarce resource to race over (many customers can redeem the same code simultaneously; the only invariant is per-customer, serialized by their own row).
- **Where it's redeemed in the app**: a single collapsible "Have a promo code?" card on the Rent screen (`#screen-code`), positioned between the item-entry card and the PAY & RENT button (moved here 2026-07-14 from beside the cabinet code, per staff feedback) — deliberately NOT on the signup form and NOT a separate Account-screen field, since every customer (brand new right after signup, or returning) passes through this exact screen every time they're about to rent. `renderPromoSection()` (shared by `goCode()`'s full reset and `submitPromoCode()`'s in-place refresh) hides the card entirely once `member.promo_redeemed` is true and renders a "Your next rental is free!" banner when `member.free_rental_credits > 0`.
- **Applying the credit**: `doRentStart` checks `free_rental_credits > 0` and zeroes `base_price` for that one rental if so (stamped as the literal number `0`, not blank) — this **wins over any live Rental Promo discount** rather than stacking with it. The extended rental fee (`daily_rate`) is completely untouched either way, per the "still charge $2/day" decision; a separately-active Rental Promo can still discount the daily rate for everyone, independent of one customer's free base.
- **The credit is consumed at `doRentConfirm`, not at `doRentStart`** — deliberately. If it were consumed the moment the pending rental is created, an abandoned Stripe Checkout (customer never completes it; the pending row auto-voids after 30 min like any other) would burn the credit for nothing. Waiting until the rental actually activates means only a genuinely completed free rental ever costs a credit. `doRentConfirm` detects a free rental via `parseFloat(base_price) === 0` rather than a separate flag — safe because a Rental Promo discount can never itself produce exactly 0 (it floors at $1), so that value is an unambiguous signal.
- **A real bug this depended on fixing**: `computeAccrued_` used to do `parseFloat(rental.base_price) || 0`, which can't distinguish a blank cell from a deliberately-stamped `0` — both produce `0` through that expression — and blank was being treated as "use the item's full price." A genuinely free rental would have silently been charged the normal price. Fixed to check the raw cell value for blankness first.
- **Stripe mechanics** (mirrors the self-service card-update pattern): if the customer already has a card on file, `start-rental.js` confirms the free rental directly with no Stripe call at all (same shape as the staff-comp path). If they don't yet (their first-ever rental happens to be the free one), it opens a Stripe Checkout **setup-mode** session — collects and saves a card exactly like a normal first rental, but charges nothing — tagged `sfd_kind: 'free_promo_rental'` in the session metadata. `rental-webhook.js` recognizes that tag on completion, sets the new card as the invoice default (same as the card-update webhook branch), and calls `rent_confirm` instead of `rent_card_updated`.
- **Known accepted gap, not fixed**: `doRentStart` doesn't limit how many *pending* (not yet confirmed) rentals a customer can have at once — only active ones count against `rental_limit`, a pre-existing characteristic of the system unrelated to this feature. In the pathological case of two pending free rentals both getting confirmed from one credit (e.g. two open tabs), the credit floors at 0 rather than going negative, but the customer could end up with two free discs from one credit. Not solved here — consistent with the rest of the codebase's tolerance for narrow, deliberate-effort-required edge cases over added locking complexity.

### Blacklist and cabinet code timing
- The general-collection cabinet code is handed to a customer **as soon as their account is created** — `getCustomer()` reveals it whenever `status === 'active'`, with no rental required first. (The vault code, if this ever grows one, stays separately gated — untouched by this.)
- A new `Blacklist` tab (`blacklist_id, email, phone, reason, added_by, added_date`) is checked at signup by `isBlacklisted_()` (case/whitespace-insensitive on email, digits-only on phone, so formatting differences like dashes or parens still match). A match doesn't reject the signup outright — it still creates the account (so staff have a record and a legitimate person isn't silently locked out with zero explanation) but writes `status: 'flagged'` instead of `'active'`. A flagged account gets no cabinet code and can't rent (`doRentStart` already requires `status === 'active'`); the app shows the same generic "Account paused" banner as any other non-active status, without revealing why. Today the staff override is a direct edit of that customer's `status` cell back to `'active'`; the staff terminal's Rental Customers search flags a blacklisted account with a red "FLAGGED — REVIEW" badge so it isn't missed.
- This only stops a **repeat** offender from cycling through disposable contact info to sign up again — it can't retroactively catch a first-time bad actor whose payment hasn't happened yet.

### Billing policy (fees vs locked-card risk)
- **At rental:** base charged immediately. First rental via Stripe Checkout (`start-rental` returns a URL; card saved with `setup_future_usage`); later rentals are one-tap off-session charges. Proves the card is live before the disc leaves.
- **At return:** staff CHARGE & CLOSE bills the accrued overage in one transaction.
- **While out:** RUN BILLING SWEEP (staff terminal) charges any active rental ≥7 days since last charge or ≥$10 owed (`due_for_sweep` flag from `rental_billing`). Bounds a locked-card loss to roughly one week.
- **Failed charge:** customer `payment_status=failed`, blocked from new rentals, banner in the app, retried by the next sweep.
- **Email receipts (added 2026-07-15):** the very first rental goes through a real Stripe Checkout Session, so Stripe can auto-email a receipt for that one charge if the account's "email customers about successful payments" setting is on. Every charge after that (a returning customer's one-tap rental, and every CHARGE & CLOSE / billing-sweep extended-fee charge) is a raw `stripe.paymentIntents.create()` call — Stripe does **not** infer the email from the attached Stripe Customer for these, so `receipt_email` must be set explicitly or no receipt ever goes out. Both off-session charge sites (`start-rental.js`'s returning-customer branch, and the `chargeOneRental()` shared by `charge-rental.js`/`billing-sweep.js`) now pass it — the base charge already had the email from `rent_start`'s response, but the sweep/CHARGE & CLOSE path needed `rent_charge_lookup` to start returning the customer's `email` too (added there for exactly this). No app-facing behavior changes if a customer has no email on file — `receipt_email` is simply left `undefined`, same as before this change.
- **Rental reminder emails (added 2026-07-20):** a daily Apps Script time-driven trigger (`sendRentalReminders`, created once via `setupReminderTrigger` from the editor; runs ~9am script-timezone) emails every ACTIVE rental at three stages, using the same ceil-of-hours day math as billing so the schedule tracks the included window automatically: day 5 ("due in 2 days"), day 7 ("due back today"), day 8 ("extended rental fee now applies" — the ADVERTISED boundary; the unadvertised grace day means accrual really starts day 9, so anyone acting on the day-8 nudge still pays nothing extra, and the buffer stays unadvertised). The day-8 email quotes the rental's own stamped promo `daily_rate` when present. Sends go through the SFD Mailer relay and are recorded in a `reminders_sent` column on the Rentals row (stage names appended, e.g. `due_soon,due_today`); if that column is missing the exact-day match still caps each stage at one send per daily run, so the worst failure is a duplicate on a manual re-run, never daily spam. Comp accounts are skipped. Settings key `reminder_emails` = `off` pauses everything without deleting the trigger. Rationale: most people are not trying to keep a movie, they forget — reminders attack accidental kept-disc cap-outs directly; the $1/day rate question stays parked until post-reminder data exists (decision 2026-07-20).

### Rental Stats (added 2026-07-20)
A full rebuild of the `Dashboard` tab on every refresh — CATALOG, REQUESTS, and RENTALS sections, computed by reading each tab by header **name** (`sheetToObjects`, same as the rest of this backend) instead of the original tab's hardcoded spreadsheet formulas, which is exactly what let the old CATALOG numbers silently keep reading a renamed tab (`'Catalog Demo'`) after the real catalog swap with nobody noticing. The **MEMBERS** section from the original hand-built tab is deliberately dropped (member/deposit model is dormant). Trade-off accepted deliberately: Catalog/Requests numbers now update on the same daily cycle as Rentals (or a manual `refreshRentalStats` re-run) rather than instantly on open, the way the old live formulas did. Reports: number of rentals (all-time), average rental length, percentage returned on time, average days overdue (late-but-physically-returned only, not kept discs), total reminder emails sent, reminders followed by a return within 24 hours, average extended fee per finished rental, and a "titles that repeatedly stay out too long" list (`repeat_late_threshold` in Settings, default 2+ late returns). `computeRentalStats_()`, `computeDashboardCatalogStats_()`, and `computeDashboardRequestStats_()` are the pure-compute halves (unit-tested, no Sheet dependency); `writeDashboardSheet_()` is the Sheet-writing half. Refreshes daily via its own trigger (`setupStatsTrigger`, same pattern as the reminder trigger — run once from the editor to create it, which also populates the tab immediately rather than waiting a day). "On time" reuses the same `RENT_INCLUDED_DAYS + RENT_GRACE_DAYS` boundary as billing and `countOnTimeReturns_`; a `paid_off` (kept) rental counts toward "finished" but can never count as on-time, so it correctly drags the percentage down. The 24-hour reminder-effectiveness metric depends on `reminders_sent` entries carrying a timestamp (`stage:ISOtimestamp`, added the same day) — entries from before that change are bare stage names and are safely ignored by that one metric (they still dedupe correctly and still count in the raw reminders-sent total).
- **Section banners + charts, rebuilt on every refresh (added 2026-07-21):** `writeDashboardSheet_` captures each section's header row as a variable (`catalogHeaderRow`/`requestsHeaderRow`/`rentalsHeaderRow`) at the point it's written into the `rows` array, the same technique `repeatHeaderRow` already used — never a hardcoded row literal, so a stat added to any section later can't silently drift the banner or chart positions out of sync (the exact fragility this tab moved off of in the first place). Each header row gets a full-width colored background + white bold text (`DASHBOARD_SECTION_COLORS`: CATALOG blue `#4a86e8`, REQUESTS orange `#e69138`, RENTALS green `#6aa84f`) rather than shading every data row, since RENTALS' length varies with how many repeat-late titles exist. `writeDashboardCharts_(sheet, rentalStats, catalogHeaderRow)` runs at the end of the same function: any chart left over from a prior run is removed first (`sheet.getCharts().forEach(removeChart)` — `sheet.clear()` does not touch floating chart objects, only cell content/formatting), then a Catalog Breakdown pie chart is always inserted (built straight from the Available/Checked out/Return bin cells already written, positioned at column F so it never overlaps the text columns), and a Titles Returned Late Repeatedly bar chart is inserted only when `repeat_late_titles` is non-empty. That bar chart reads from a small separate numbers-only staging block in columns F/G rather than column C's existing "3 late returns" human-readable text — a chart needs a real number, and this avoids reformatting what staff already see in that column.
- **Sales tax (added 2026-07-15):** NJ treats a disc rental as a taxable "lease or rental" of tangible personal property, so the standard state rate applies to both the base price and any extended-day charge. `salesTaxRate_()`/`taxCents_()` in the Apps Script read a new Settings key (`sales_tax_rate`, defaulting to 6.625 if unset — a sheet edit if the rate or jurisdiction ever changes, not a redeploy) and feed `tax_cents` on `rent_start`'s and `rent_charge_lookup`'s responses. **Deliberately Stripe-side only**, per an explicit scope decision: tax shows as its own line item on the first rental's Checkout Session and is folded into what both off-session charge sites (`start-rental.js`'s returning-customer branch, `chargeOneRental()`) actually charge — but it never touches `computeAccrued_`, the payoff cap, or anything recorded as "paid" in Rentals (`rent_charge_recorded` still reports the pre-tax `owed_now_cents`). Tax collected on the state's behalf isn't SFD's revenue and shouldn't blend into the same numbers the cap/accrual math already uses — standard practice, not a shortcut. The app's own price displays (item-confirm card, Browse, success receipt, My Rentals) still show pre-tax **numbers**, but as of 2026-07-17 every customer-facing price quote is labeled "plus tax" (item-confirm, Browse facts, success receipt, receipt view, My Rentals/Return cost lines, the rent terms line, the signup checkbox label, the Account note, and the getting-started banner) so a one-tap saved-card charge is never larger than what the customer was told — the exact tax math still lives only in what Stripe charges and shows on its Checkout page/receipts.

### Sheet tabs
- **Customers:** `customer_id, customer_token, display_name, email, phone, terms_accepted, stripe_customer_id, payment_status(none/ok/failed), rental_limit, status(active/flagged/paused/...), comp, card_printed, joined_date, notes, promo_redeemed_date, free_rental_credits`. The last two are for Promo Codes (below): `promo_redeemed_date` blank until a code is ever redeemed (one lifetime redemption per customer, checked by `redeem_promo`); `free_rental_credits` is an integer, incremented on redemption and consumed by `doRentConfirm` when a free rental actually activates. `rental_limit` starts at Settings `default_rental_limit` (1); staff raise it per customer after a clean first return (or it self-raises via Trusted Accounts, above). **`comp=TRUE` marks a staff comp account:** rentals log exactly like anyone else's (Rentals row, catalog status, Transactions audit trail, staff Outstanding Rentals) but no money ever moves — `rent_start` returns a `comp` flag so `start-rental` skips Stripe and confirms directly (no card is ever collected), and the billing reads (`rental_billing`, `rent_charge_lookup`, the customer payload's `owed_now`) report 0 owed so the sweep and CHARGE & CLOSE naturally collect nothing. The app shows a "Staff account" banner and no-charge cost lines; the staff terminal badges these rows "STAFF — NO CHARGE". Set it by typing TRUE in the sheet; there is no signup path to it. **`status: 'paused'`** is set automatically when one of the customer's rentals hits its maximum charge (see Kept Discs below) — blocks new rentals and hides the cabinet code, same as any inactive status; cleared only by staff tapping REACTIVATE ACCOUNT in the terminal (fully manual, no auto-reactivate). **`card_printed`** (TRUE/blank) drives the staff terminal's batch card-printing list — set by `customer_card_printed`, never by the customer.
- **Rentals:** `rental_id, customer_id, item_id, start_date, status(pending/active/return_pending/closed/paid_off/void), base_price, daily_rate, base_paid_date, extra_charged, last_charge_date, return_date, closed_date, notes, reminders_sent`. `daily_rate` (dollars) is stamped at rent time when a Rental Promo is live; blank = standard `RENT_DAILY_CENTS`. Dollars in the sheet; the code does math in cents. `pending` rows older than 30 min are auto-voided by any `rental_billing` read (and inline by `rent_start` for the same item). `paid_off` is a live, currently-produced status again as of 2026-07-10 (see Kept Discs above) — it means the customer kept the disc by default, not a legacy artifact.
- **Catalog:** + `rental_price` column; `replacement_cost` doubles as the payoff cap.
- **Blacklist:** `blacklist_id, email, phone, reason, added_by, added_date` — checked at signup only (see above); a row needs an email, a phone, or both.
- **Promo Codes:** `code, active, notes` — see the Promo Codes section above. `active=TRUE` rows are matched case/whitespace-insensitively against what a customer types; no dates, no per-code redemption count (the tab is deliberately this small — the per-customer scarcity lives on the Customers tab instead).
- **Settings:** + `default_payoff_cost`, `default_rental_limit`, `trusted_on_time_threshold`, `trusted_rental_limit`, `return_cooldown_minutes` (blank = 60; 0 disables), `sales_tax_rate` (percent, e.g. `6.625`; blank defaults to NJ's standard rate), `reminder_emails` (`off` pauses the rental reminder emails; blank/anything else = on), `repeat_late_threshold` (integer; blank defaults to 2 — how many late returns a title needs before it appears in Rental Stats' repeat-offender list).

### Endpoints
- GET `customer&key=&token=` — dashboard payload (profile, open rentals with live accrual, history, `cabinet_code` as soon as the account is active, `active_promo` from the Rental Promos tab, location).
- POST (api_key): `customer_signup` (**requires both** email and phone as of 2026-07-13 — the pair powers lookup recovery and the email receives the account link; rejects a duplicate email or phone already on another Customers row — normalized the same way as the Blacklist match — with an error pointing the customer at the recovery flows instead of silently creating a second account; **emails the personal account link at signup** via `sendAccountLinkEmail_`, fire-and-forget, `link_emailed` boolean in the response — mail relays through the separate **SFD Mailer** Apps Script in the shop's Gmail account (`MAILER_URL`/`MAILER_KEY` constants; direct MailApp from the main script is stamped from the aol.com identity and fails DMARC everywhere, see BACKEND-UPDATE.md); also returns `cabinet_code` and a `flagged` boolean so the app can show the code immediately or the pending-review message), `customer_lookup` (requires **both** email and phone to match the same account, returns `customer_token`; still live server-side but **retired from the rent.html UI 2026-07-13** in favor of `send_link`), `send_link` (self-service recovery: mails the account link **to the address on file** when the submitted email matches; response is identical match or no-match so it can't probe registered emails; 10-min per-address cooldown via CacheService; base URL = Settings `rent_app_url`, blank → `https://scifidojo.com/rent`), `redeem_promo` (Promo Codes, added 2026-07-14: generic reusable codes, one lifetime redemption per customer via `promo_redeemed_date`, not single-use per code; adds a `free_rental_credits` count; see the Promo Codes section above), `rent_start` (validates limit/payment/item — vault titles are rejected since customers never get the vault code; a fresh `pending` row on the same item by someone else blocks it with a "checking out right now" error, while the customer's own pending rows and anyone's stale ones (>30 min) are voided inline rather than waiting for a staff `rental_billing` read; creates pending rental; returns price + has_card; **only ACTIVE rentals count against the limit** — a bin-pending return no longer holds the slot, decision 2026-07-10), `rental_return` (freezes accrual, item → return_pending; **return cooldown**: blocked until `return_cooldown_minutes` (Settings, default 60) after checkout so slots can't be insta-cycled — staff comp accounts exempt, and a valid `staff_pin` in the payload overrides the cooldown AND skips the token/ownership check, which is how the staff terminal's LOG RETURN (STAFF) button works).
- POST (SERVER_KEY, Netlify only): `rent_confirm` (payment done → active, clock starts, item checked_out; only transitions `pending`/`void` rows — a Stripe webhook redelivered after the rental closed is acknowledged without reopening it), `rent_charge_lookup`, `rent_charge_recorded` (adds charge; closes + reshelves on a real return, or auto-closes as `paid_off` + pauses the account when a non-return charge reaches the cap), `rental_payment_failed`, `rent_card_lookup` (token → customer_id/stripe_customer_id/payment_status/comp; feeds the update-card Netlify function — the Stripe id never rides a public payload), `rent_card_updated` (webhook → clears `payment_status` to `ok` after a setup-mode Checkout saves a new card; idempotent, dated note written once per day).
- GET (staff_pin): `all_customers` (now includes `card_printed`), `rental_billing` (open + kept rentals, owed_now, flags). POST (staff_pin): `rental_close` (waive & close), `process_kept_return` (walk-in return of a kept disc: refund noted for the Stripe dashboard, catalog reactivate choice), `reactivate_customer` (clears a `paused` status), `customer_card_printed` (marks a signup's card as printed for the batch-printing list).
- **Bin-outcome actions (added 2026-07-21, all staff_pin, no Stripe, internally gated like `process_kept_return`; each requires the rental be `return_pending` and writes a Transactions audit row):**
  - `rental_undo_return` — `{ rental_id, staff_pin }`. The exact inverse of `doRentalReturn` for a return logged by mistake: rental → `active` + `return_date` cleared (so `computeAccrued_` restarts the clock), catalog item → `checked_out` with the holder re-asserted. Fully reversible; no customer change. Audit prefix `RU`.
  - `rental_mark_mia` — `{ rental_id, staff_pin, note }`. A logged-returned disc that never turned up: rental → `closed` + dated MIA note, catalog item → `status: missing`, `in_rotation: FALSE`, holder cleared. **Auto-pauses the customer** via the generic `pauseCustomer_` helper (same skip-if-`flagged` pattern as `maybeAutoPauseForPayoff_`, but a distinct MIA-worded note — reusing the payoff helper would falsely stamp "reached its maximum charge"). Audit prefix `MI`.
  - `rental_mark_damaged` — `{ rental_id, staff_pin, note }`. Disc came back unusable: same as MIA but catalog `status: damaged` and **no auto-pause** (damage is a case-by-case call staff flag manually). Audit prefix `MD`. Shares `markRentalUnrecovered_` with MIA (the only differences are catalog status, the auto-pause flag, and the note label). New catalog `status` values in play: `missing`, `damaged` (both out of rotation).
  - **Staff-terminal UI for these three is the sister (onboarding) session's job** — this session only ships the backend actions; the buttons that call them live in `sfd-staff.html`.
- Catalog/events/perks/updates/member_request accept customer tokens too (`accountForToken_`); rental audit rows land in Transactions as `rental_*` actions (invisible to legacy member views).

### Returning-customer login (rent.html)
- The signup screen leads with a Returning Customer card: **SCAN MY CARD** opens the same camera/jsQR pipeline used for item lookup, now parameterized by `cameraPurpose` (`'checkout'` vs `'login'`) so `openCamera()`/`onQRDetected()` branch correctly — a login scan extracts a `token` query param if the scan is a URL, else treats the raw scanned text as the token itself, then calls `loginWithToken()`.
- **EMAIL ME MY LINK is the only non-scan recovery path** (2026-07-13): a collapsible card (reusing the `toggleNote()` accordion) takes just an email; `submitSendLink()` posts it to `send_link`, which mails the account link to the on-file address (never the requester's input as a destination beyond matching). The app always shows the same "If that email is on an account..." confirmation, mirroring the backend's identical-either-way response. Signup also emails the link automatically (`link_emailed` drives a "We also emailed it to you" line on the success screen), and signup requires **both** email and phone client-side and server-side.
- The old **email+phone instant login (FIND MY ACCOUNT) is retired from the UI** (same day): inbox possession is stronger proof than knowing two facts about someone. `submitCustomerLookup()` is left defined but unused (standard retire-in-place convention) and the backend `customer_lookup` action is still live, so the form can come back with a pure HTML change if ever wanted.
- `loginWithToken()` verifies the token against `?action=customer` before committing to it, so a bad scan or a stale/garbage token shows an inline error and stays on the signup screen instead of bouncing to the full-page "Account Link Invalid" error.

### SFD card printing (staff terminal)
- `sfd-staff.html` → Rental Customers → **PRINT CARD** on any result row. `printCustomerCard()` looks the customer up in the already-loaded `allCustomers` list and calls `buildCustomerPrintUrl()`, which mirrors the legacy `buildPrintUrl()` pattern one-for-one: `cards/print-cards.html?token_a=<customer_token>&name_a=<display_name>&id_a=<customer_id>&since_a=<formatSince(joined_date)>`. No `reprint_a` flag on first print (matches the legacy default-print call).
- `cards/print-cards.html` picks the QR base URL by token prefix (fixed 2026-07-06): `cus_` tokens encode `scifidojo.com/rent?token=`, everything else the legacy `/member?token=`. Customer cards also say "unique to your account" (not "membership") and "Since <year>" (not "Member since"). The card FRONT art still carries member-era labels ("+ Member Access", "Founding Member Series +") baked into the overlay/PNG — changing those is a design call, not wiring.
- Typical flow: customer creates their SFD Account digitally on visit one (QR → signup → bookmarked link, no physical card yet); staff print the card whenever the customer is next at the counter.

### rent.html notes
- Fork of member.html; internal variable is still `member` — `normalizeCustomer()` aliases `member_id`, splits open rentals into `rentals` (active) + `return_pending`, sets `tier_limit = rental_limit`.
- Rent flow calls the Netlify `start-rental` (CORS-enabled): `{url}` → redirect to Stripe Checkout, `{charged:true}` → instant success. Back-from-Stripe (`?rented=1`) polls the customer endpoint briefly for webhook lag.
- **Browse is discovery-only** (no RENT button — renting starts by scanning the disc in hand at the cabinet). Rows tap-to-expand (`browseExpanded` map lives outside the render so star-toggle re-renders keep rows open) into a detail panel: cover art loaded **on demand from `/covers/<item_id>.jpg`** (repo-committed by filename convention, no sheet column — see `/covers/README.md`; the `<img>` element only exists while a row is open, so covers never preload, and `coverMissing` suppresses re-requests for 404'd images), a synopsis (added 2026-07-18, `browseDetailHtml()`'s `synopsisHtml` — staff-curated `synopsis` catalog column, hide-if-blank, rendered as its own paragraph above the facts list since it's freeform sentence-length text rather than a short label), a price line (`rental_price` rides the `catalog` payload since the 2026-07-08 backend; the line is hidden when the field is absent so an old deploy never shows a guessed price), a maximum-charge fact (`cap_cents`, added 2026-07-13 — the backend's resolved `payoffCapCents_` value, same hide-if-absent rule as the price line), subcategory/disc-count/condition facts (defaults `standard`/`good` are hidden as noise), and an availability line. **`group_name`'s "Part of: [group_name]" fact was added 2026-07-21, then removed the same day (2026-07-21b):** it usually just repeated the item's own title back (a box-set disc's `group_name` is typically identical or near-identical to its own `title`, e.g. "Alien Quadrilogy" showing "Part of: Alien Quadrilogy"), so it read as noise rather than the box-set-member clarification it was meant to be; `group_name` is still sent by the backend and still available for a future, more targeted use, just not rendered as a Browse fact right now. **Subcategory now reads as a formatted label, not the raw sheet value** (`SUBCATEGORY_DISPLAY_NAMES`: `box_set` → "Box Set", `tv_series` → "TV Series", `criterion` → "Criterion Collection"; `standard` stays hidden as before) — same display-only-map pattern as `GENRE_DISPLAY_NAMES`, the underlying value is untouched. **Cover art no longer stretches for box sets/TV series (fixed 2026-07-21):** `.browse-detail` is `display:flex`, and its default `align-items:stretch` was silently distorting `.browse-cover` vertically to match the height of the sibling facts column — invisible on a plain single-disc item (short facts list, cover already near its natural height) but visibly elongated on a box set or TV series, whose extra facts (non-`standard` subcategory, `disc_count > 1`, and — at the time the bug was found — the since-removed "Part of:" line) push that column much taller. Fixed with `align-self:flex-start` on `.browse-cover`, confirmed with a Playwright test that renders the identical source image next to a short vs. long facts list and asserts both render at the same, aspect-ratio-correct height. `.ticket-cover` was never at risk — its parent (`.ticket`) isn't a flex container. **Price is two-tier, matching the item-confirm card (2026-07-21):** a bold `.browse-price` headline ("$3 <span class=browse-price-sub>plus tax · 7 days</span>") sits above the facts list, with the extended-fee rate and maximum charge merged into a single fine-print fact ("Then $2/day extended rental fee if kept, up to $27 plus tax maximum total charge.") instead of two separate lines — same hide-if-absent behavior per field (an old deploy with `cap_cents` but no `rental_price`, or vice versa, still shows whichever it has rather than nothing).
- **Genre filter (added 2026-07-21):** a second, wrapping row of `.filter-btn` pills (`#browseGenreFilters`, `.genre-filters` CSS modifier for `flex-wrap`) sits below the existing All/Available Only/Starred row, reusing that same pill style rather than introducing a new control (there is no `<select>` anywhere in this app). **Collapsible behind a "Filter by Genre" toggle**, same day: 28 distinct genres made an always-visible chip cloud too tall for the screen, so the chips now live inside a `.note-fields`/`.note-toggle` accordion (`noteToggleGenre`/`noteFieldsGenre`, the exact mechanism the Hours & Location and promo-code cards already use — `toggleNote('Genre')`, chevron rotate, `role="button"`/keyboard support all come free from the existing generic delegated keydown handler), collapsed by default. `renderGenreFilters()` computes the distinct genre list fresh from `catalogCache` on every render (cheap at ~700 items) rather than caching it, and always prepends an "All Genres" reset chip. `setBrowseGenreFilter(g)` sets `browseGenreFilter` and re-renders (the panel's open/closed state isn't touched by this, same as the promo-code accordion). The filter is a strict AND with the existing status filter and search box. Items with an empty `genre` array (53 of 689 titles, mostly box sets or simply ungenred) never match a specific chip but always show under "All Genres" — no `undefined`, no crash, since matching is array-guarded (`Array.isArray(it.genre) && it.genre.indexOf(...)`). `goBrowse()` resets `browseGenreFilter = ''` alongside its existing filter/search reset. Backend: `getCatalog` parses the Catalog tab's comma-separated `genre` column into a trimmed array per item (see the Catalog tab section above). **Chip labels are shortened for the couple of genre names too long to read comfortably** (`GENRE_DISPLAY_NAMES`: "Science-Fiction" → "Sci-Fi", "Suspense/Thriller" → "Thriller") — display-only, the chip's `data-genre` and the actual filter match still use the full string, so nothing about matching changes. **Known data-quality note, not a code issue:** a small minority of titles (19 of 636 genred titles, ~3%, e.g. Akira at 8 genres) carry enough genre tags that genre filtering isn't very discriminating for them specifically — deliberately not capped or trimmed client-side, since silently dropping sheet-entered genres would diverge from what staff typed; a cleanup pass on those rows is a staff/sheet task, not a rent.html one.
- **Maximum charge is now shown before paying, not just in Rentals history** (2026-07-13): the item-confirm card (`lookupItem()`) says "...then $2 a day, up to a maximum charge of $X" using the item's `cap_cents`; Browse shows the same figure as a detail fact. Previously this number only appeared after the fact in My Rentals ("yours to keep at $X") and, since the same day, on the Stripe Checkout page itself.
- **Active promo pricing shows a "was $X, now $Y" strikethrough** (2026-07-14) in both the item-confirm card and Browse's price fact — for the base price when `item.rental_price_was` is present (a base-discount promo is live) and for the extended rental fee when `dayRatePromoActive()` says the live day rate undercuts `standardDayRateCents()` (sent explicitly by the backend as `active_promo.standard_daily_cents`, not hardcoded). `promoPriceHtml()` is the shared helper; with no live promo neither number shows a strikethrough and everything renders exactly as before.
- **Item-confirm card is two-tier, not one dense paragraph** (2026-07-21): the base price/promo credit renders as a bold `.ic-price` headline ("$3 <span class=ic-price-sub>plus tax · 7 days</span>" or "Free!"), with the extended-fee rate, maximum charge, and card note all folded into the smaller `.ic-msg` fine print below it — same facts as before (nothing removed, see the maximum-charge-disclosure decision above), just not all at the same visual weight as the price a first-time customer is deciding on. `itemConfirmCard()` takes an optional 4th `headline` param (comp accounts skip it entirely — no price to headline). Same day, the fine print gained one more line: **"We will email you a reminder before it is due"** — a reassurance that pairs with the rental-reminder-email feature (2026-07-20) so the pricing card reads less like a wall of fees and more like "we've got you covered." Comp accounts don't get this line (their rentals are skipped by the reminder job); free-credit and saved-card rentals both get it.
- **Promo code redemption lives on the Rent screen, one field for everyone** (2026-07-14): a collapsible "Have a promo code?" card sits on `#screen-code` between the item-entry card and PAY & RENT (moved here same day, per staff feedback, from beside the cabinet code — its original spot), same `toggleNote()` accordion mechanism as Hours & Location — deliberately not on the signup form and not a separate Account-screen field, since new customers land on this exact screen right after signup (`goFirstRental()`) and returning customers land here every time they tap RENT. `submitPromoCode()` posts to `redeem_promo`, then re-fetches (`refreshAfterRent()`) and refreshes just `renderPromoSection()` plus (if an item is already entered) a `lookupItem()` re-call to update its price line in place — **not** a full `goCode()`, which would otherwise wipe an already-scanned item now that the card sits below item entry rather than above it. `lookupItem()`'s price line, the PAY & RENT button text, and the success receipt all branch on the credit too ("Free!" / "CONFIRM FREE RENTAL" / "Free (promo credit)") — the free/normal branching happens in three places, not one shared helper, since each has different surrounding copy.
- **Due-back date now shows for active rentals, not just the Return Bin** (2026-07-13): a shared `dueDateStr()` helper (`start_date + 7 days`, client-side, no backend field needed) is used by the Return Bin section, the Return screen's selectable active-rental list, and My Rentals' active rows ("Due back Jul 20 · Day 3 · ..."). Return-pending rows in My Rentals don't get a due date — they've already been returned, so it isn't meaningful there; the Return Bin section already covers them.
- **Client error reporting** (2026-07-14, for the unstaffed store — there's a person on-site but no one who can give an accurate description of what broke): `reportClientError(context, err, extra)` fire-and-forget POSTs to the new `client_error` backend action whenever something real fails — `window.onerror`, a new `window.onunhandledrejection` handler (nothing caught these before), and the catch blocks of `submitSignup`, `submitRent`, `submitReturn`, `goUpdateCard`, `submitSendLink`, `refreshAfterRent`, and `loadMember`'s two failure branches (skips the "bad/expired token" branch — that's just a stale link, not a bug). Deliberately excludes routine validation rejections that already show the customer a clear message. Backend rate-limits to keep this from becoming its own noise source (see below); reporting failures are always swallowed so they can never affect what the customer sees.
- **Server-side exception alerting** (2026-07-14, closes the gap the client-side reporting above can't reach): a raw exception thrown inside `doGet`/`doPost` itself — a bad column name, a Sheets quota error, a malformed POST body — used to just return a JSON error with no one finding out. Their top-level `catch` blocks now also call the shared `alertAllowed_()`/`sendMail_()` pair, so **every** action is covered, not just the ones the app happens to report — including the server-to-server calls from Netlify (`onboard`, `update_stripe`, `rent_confirm`, etc.) that `client_error` can never see. `alertAllowed_()`'s dedup key strips item/rental/customer-style codes (`SFD-0042` → `#`) before hashing, so the same underlying failure recurring across different items/customers still coalesces into one alert instead of each "unique" code defeating the cooldown and burning the shared hourly cap. Deliberately does NOT keyword-filter out routine messages like "not found" — a filter broad enough to catch that would also swallow "Column not found: X", which is the exact scenario this exists to catch. Accepted tradeoff: routine validation rejections can occasionally alert too (rate-limited same as everything else), rather than risk hiding a real bug behind a clever-looking filter.
- **App status strip (`#appStatusBar`)** is a fixed chip pinned to the top of the viewport (the old `#catalogStatusBar` at the bottom of the dashboard is gone): catalog loading/ready/failed states live there, plus an "UPDATING ACCOUNT..." chip (`appBusy()`, counted so overlapping refreshes don't clear each other) whenever a fresh account fetch is in flight. `refreshAfterRent()` runs after every money action — rentals AND logged returns — so the customer always lands back on server truth.
- **`showScreen()` is a no-op when the target screen is already active.** Background re-renders (catalog finishing, account refreshes) used to route through it and yank the scroll position to the top mid-read; only real navigation scrolls/refocuses now. Don't "fix" this back.
- **Return cooldown UI:** the return list greys out rentals younger than `member.return_cooldown_min` (payload field; server enforces regardless) with an unlock-in-N-minutes note and an ask-staff hint; `toggleReturn()` ignores taps on locked rows.
- **Dashboard is trim, four tiles only** (decision 2026-07-10): RENT, RETURN, BROWSE, ACCOUNT. The old six-tile grid (RENT/RETURN/MY RENTALS/ACCESS CABINET/BROWSE/ACCOUNT), the account-status stat grid, and the Return Bin notification banner are gone from the home screen — moved into Account (status detail, My Rentals) or the Return screen (bin visibility), or folded into a tile subtitle (RENT shows "N available" / "Limit reached" directly, computed the same way `backendGetMemberStatus()` always has). (`statCard()`/`statCardFull()` were left defined but unused until the 2026-07-17 dead-code cleanup removed them — see Removed below.)
- **All four tiles gray out during the catalog LOADING flash (added 2026-07-21, refined 2026-07-21b):** previously only RENT grayed out while `catalogCache` was still loading (BROWSE/ACCOUNT always tappable, RETURN gated only on `member.rentals.length > 0`) — a half-grayed grid read as confusing. During the transient **loading** state all four now gray out together, covered by a single `.action-grid-overlay` spinner over `#actionGrid` (shown only while `catalogLoading`, i.e. `catalogStatus !== 'failed' && catalogCache === null`). **On catalog FAILURE the gate is deliberately narrower:** RENT and BROWSE genuinely need the catalog (RENT validates the scanned item against `catalogByIdCache`; BROWSE *is* the catalog) so they stay disabled with a "Catalog unavailable" sub-label, but RETURN reverts to its own `canReturn` rule and ACCOUNT to always-on — because neither touches the catalog, and a *persistent* outage must never lock a customer out of logging a return (which freezes extended-fee accrual) or reaching their account. The failure overlay was removed precisely so it can't block those two tiles; the retry lives in the top status chip (`setCatalogStatus`'s "CATALOG: FAILED — TAP TO RETRY", `onclick retryCatalog()`). Once the catalog is ready, each tile's own rule applies (RETURN disabled with nothing out; RENT respects `st.canCheckout`). The earlier "all four gated on `catalogReady` incl. failure" version locked RETURN/ACCOUNT out during an outage — a real bug, hence the 2026-07-21b refinement.
- **Active-rental reminder card, revived 2026-07-15** (partial rollback of the above — the trim went a bit too far): `renderCurrentRentals()` was retired-in-place on 2026-07-10 and is now live again, rebuilt onto `#currentRentals` (sits right below the notifications section, above the action tiles). One tappable `.stat-card` per entry in `member.rentals` (active only — a `return_pending` disc already has its own visibility via the Return screen's bin section, so it's deliberately excluded here); renders nothing at all for a customer with zero active rentals, so a brand-new account sees no extra clutter. Tapping a card calls the new `showRentalReceipt(rentalId)`, which reuses the existing `showSuccess()` screen (the same one shown right after paying) to show a full-page confirmation — item, rented/due-back or returned/closed dates, amount paid, maximum charge, and cabinet code for an active rental — so a customer can always pull up proof of a specific rental if the store ever questions whether they paid for it, not just in the moment right after checkout. The same `showRentalReceipt()` is wired into My Rentals' active/return_pending rows (the whole `.rental-item` is now a tap target, no other buttons live inside it) and into rental history via a VIEW RECEIPT button alongside FLAG ISSUE (a separate button rather than making `.hist-item` itself tappable, since buttons already live inside it and a wrapping click handler would double-fire on them; history's one-tap LEAVE REVIEW was removed 2026-07-19 — see the review card below). **REPORT AN ISSUE mailto link (added 2026-07-17):** the receipt view also shows a `mailto:scifidojo@aol.com` link, but only while the rental is still open (active or return_pending) — a `#sucReportLink` div in the `screen-success` template, populated by `showRentalReceipt()` and cleared by `showSuccess()` itself so the post-payment/return-logged screens never inherit a stale link from a prior receipt view. Subject/body are prefilled with the item's SFD code and the customer's CUS id, plus a bracketed `Issue: [...]` prompt the customer edits before sending (disc won't play, disc missing, etc.) — a real email, not the backend-logged `member_request`/flag action, since an open rental's problem (still holding a bad disc) usually needs an actual reply, unlike history's FLAG ISSUE which stays a queued note for staff to triage later. Deliberately not shown for closed/paid_off rentals, which already have FLAG ISSUE for exactly that purpose. **Leave a Review card, same day:** a second collapsible control right below it (`#sucReviewCard`, cleared by `showSuccess()` the same way), reusing the generic `toggleNote()` accordion. **Shown for every rental in the receipt view, open or closed, as of 2026-07-19** — it replaced history's old one-tap LEAVE REVIEW button entirely (that button posted `review` rows with no text at all, which read as junk in Requests once real reviews existed; `histAction()` is now flag-only). Same day, the receipt's PAID SO FAR row was corrected to show `r.paid` (actually charged) rather than `r.owed_total` (accrued) — the two diverge between day 9 and the next sweep, and a proof-of-payment screen must never claim more than was charged. **Ticket stub (added 2026-07-20):** the instant-payment success screen and the OPEN-rental receipt view render an achievement-style admit-one ticket (`renderRentalTicket()` into `#sucTicket`, cleared by `showSuccess()` like the report/review extras): SCI-FI DOJO / ADMIT ONE header, cover art (same `/covers/<item_id>.jpg` convention + `coverFail` 404 handling as Browse), NOW SHOWING + title, a dashed tear line with side notches, and an honest per-rental ordinal (`rentalOrdinal_()` sorts everything on the account by `start_date` — not just a total count, so an old rental's receipt shows its true number; when the just-confirmed rental hasn't landed in the refreshed payload yet — the race `submitRent`'s synthetic-row fallback exists for — the ordinal falls back to count+1, fixed 2026-07-20 from a bare count that could read "RENTAL NO. 0"). One-time CSS shine sweep on entry. Deliberately NO share button or "show this to someone" copy — the point is an object worth showing, not an instruction to show it. Closed rentals keep the plain receipt (a NOW SHOWING stub for a returned disc would read wrong), and the Stripe-redirect first rental never sees the instant success screen, so its ticket lives in the receipt view reached from the dashboard reminder card. Styled as a `feed-btn` rather than a `.card` — collapsed, it's pixel-identical (same width/height) to REPORT AN ISSUE above it, reading as a matched pair; only the rotating "+" chevron marks it as expandable rather than a direct action (the chevron's font-size is trimmed from the usual 18px to 14px so its taller glyph doesn't stretch the button past REPORT AN ISSUE's height). Unlike history's existing LEAVE REVIEW button (`histAction`, a single tap that posts `member_request`/'review' with no text at all), this one has an actual textarea and posts what the customer typed as `title_text` via the same `postMemberRequest('review', itemId, text)` call — no backend change needed, since `doMemberRequest` already accepted a `text` field that the old button just never sent. Blocks an empty submit client-side; on success the field collapses into a "Thanks for the review!" message in place. History's one-tap version is untouched.
- **RENT is one screen, not two** (merged 2026-07-12): the cabinet code and the item-code/QR entry both live on `#screen-code` together — `goCode()` (called by `goRentFlow()`/`goFirstRental()`) renders the code, resets the item-entry fields (folded in from the retired `goCheckout()`), and shows the screen; there is no more CONTINUE TO RENT A DISC hop or separate `#screen-checkout`. The code needs no gating beyond the existing `active` check since it's handed out at signup regardless of any rental. Back-navigation from any screen still jumps straight to the dashboard (no back-stack) — a deliberate simplicity tradeoff, not an oversight.
- **General Collection code box is green, not accent cyan** (2026-07-20, rent.html only — member.html's `.code-display` is untouched, legacy/dormant): `.code-display`'s border/label/value now use `--success` instead of `--accent`, reading as an access-granted signal. The `.vault` modifier is untouched (stays purple) so the two remain visually distinct.
- **First-visit walkthrough card (added 2026-07-20):** a dismissible 3-step card (`#walkthroughCard`, rendered by `renderWalkthrough()` inside `goCode()`) sits above the cabinet code on the Rent screen — use the code to open the cabinet, find the code on the disc, scan/type it and tap the PAY button (deliberately not "PAY & RENT" — the walkthrough's audience has no card on file, so their button reads "PAY $X VIA STRIPE"). Gated by `shouldShowWalkthrough_()`: shown only for an account that has never had a rental at all (`rentals`, `return_pending`, and `rental_history` all empty) and hasn't dismissed it (`sfd_walkthrough_seen_<customer_id>` in localStorage, set by the GOT IT button). No comp accounts. The moment an account has ever rented anything the card stops rendering on its own — the localStorage flag only matters for someone who dismisses without ever renting.
- **PAY button names Stripe when there's no card on file yet (added 2026-07-20):** `rentButtonLabel(baseCents, isFree)` is now the single source for the checkout button's text (used by `lookupItem()` and both branches of `submitRent()`'s catch block, replacing three separate inline ternaries that had started to drift) — `member.has_card === false` renders "PAY $X VIA STRIPE" / "CONFIRM FREE RENTAL VIA STRIPE" instead of the plain "...& RENT" wording, so the upcoming redirect to Stripe Checkout isn't a surprise to a first-time customer. A saved card (or a comp account) is unaffected. The in-flight "OPENING CHECKOUT..." button state was renamed "OPENING STRIPE CHECKOUT..." for the same reason.
- **Hours & Location is a tap-to-expand accordion on the Rent screen** (2026-07-12), collapsed by default (`toggleNote('HoursLoc')`, same generic mechanism used elsewhere), with the `#hoursLocCard` wrapper hidden entirely when neither hours nor an address is configured. `buildLocationHtml()` now renders hours before address (was address-first) since staff wanted open/closed status to be the first thing a member sees.
- **RETURN is action-only** (logs a return), **plus a read-only "In the Return Bin" list** appended below it via `renderReturnBinSection()` for anything already `return_pending` — title, item code, and a due-back date computed client-side as `start_date + 7 days` (no extra backend field; the included window is a fixed constant so this is exact, not an estimate), no controls, since check-in is a staff-verified physical action. My Rentals (`screen-rentals`, unchanged internals — history filters, review/flag actions) stays its own screen, reached via a button inside Account rather than a home-screen tile.
- **Events and Perks feeds are retired in rent.html** (2026-07-12): staff now post everything — events, perks, general news — as Weekly Rewind updates instead, so `#eventsSection`/`#perksSection`/`#newsUpdatesWrap` and the `loadEvents`/`loadPerks` calls are gone (the whole dead chain — `loadEvents`/`loadPerks`/`renderEventCard`/`renderPerkCard`/`toggleRsvp`/`buildGCalUrl` plus their caches, `RSVP_KEY`, and event-date helpers — was fully removed in the 2026-07-17 dead-code cleanup; the retirement is settled, so retire-in-place no longer applied). `#rewindSection` always shows the newest active update; when there's more than one, a `toggleNote('OlderUpdates')` accordion (same generic mechanism as the login screen's email/phone lookup) reveals the rest. The section renders nothing when `updatesCache` is empty. **Rewind posts can show a photo** (2026-07-14): `renderUpdateCard()` renders `u.image_url` as a `.feed-img` at the top of the expanded body when set, the same pattern (and CSS class) the retired Events/Perks cards already used — no new styling needed.
- **Account is a single Account Info card** (redesigned 2026-07-12): MY RENTALS sits at the top (above the card, primary position), then one `.card` with six `.acct-row` rows — Plan, Status, Name, Email, Phone, Card on File — replacing the old separate Free Account pill (`.member-tier.tier-mat`, no longer used in markup but the CSS is left in place, generic enough to reuse later) and the two-up status stat-grid. `.acct-row`/`.acct-row-main`/`.acct-row-sub` mirror the Hours table's row look (`acctRow()` helper) so every account fact reads as one consistent list. The customer id is no longer shown anywhere on this screen. The two mailto links are now **UPDATE INFO** and **REPORT COMPROMISED ACCOUNT / CLOSE ACCOUNT** (replacing the old generic "QUESTIONS? EMAIL US").
- **"Finalizing" banner for webhook lag/failure** (2026-07-12): arriving back from Stripe Checkout (`?rented=1`) stamps a localStorage flag (`sfd_rent_finalizing_<token>`) so a paid customer never sees a silent empty dashboard when the webhook is slow or broken. The existing ~10s poll still runs first; if the rental hasn't landed, `renderStatus()` shows a persistent "Payment received... finalizing" banner with a tap-to-refresh (`refreshFromBanner()`), adds an email fallback after 10 min, expires after 30 min, and clears itself the moment any fetch shows an active/return-pending rental. The getting-started prompt is suppressed while the flag is live.
- **Self-service card update** (2026-07-13): `goUpdateCard(el)` posts the token to the Netlify `update-card` function, which looks the customer up via `rent_card_lookup` (SERVER_KEY) and opens a Stripe Checkout **setup-mode** session (no charge) for the existing `stripe_customer_id`; errors surface inline on the tapped element ("COULD NOT OPEN - TAP TO RETRY"). Entry points: the failed-card banner (UPDATE CARD replaces "Email us"), the rent screen's card-attention alert, and an Account-screen UPDATE CARD button (hidden until `has_card`, never for comp). Returning with `?card_updated=1` polls briefly like the rent flow; while `payment_status` is still `failed` post-return the app shows a patient "can take a minute to unlock" banner (tap-to-refresh) instead of the scold, and "Card updated. You are all set." once it clears. The webhook side (setup session completed → make the new card the default / detach the old one → `rent_card_updated`) lives in the onboarding repo's `rental-webhook` function. The Account mailto is now UPDATE CONTACT INFO (contact changes only).
- **Unstaffed-store copy rule** (2026-07-12): the cabinet sits in a store whose staff are not SFD — customer-facing copy must never direct someone to on-site staff or a counter. The old "ask staff" phrasings (signup card-print line, flagged-signup NEXT row, login-scan failure, return-cooldown row, "staff check it in"/"Awaiting staff check-in") are all reworded. "Staff account" comp labels are fine (they describe the account, not a person to find). The one remaining email-only dead end (self-service card update) is queued, not solved; send-me-my-link recovery shipped 2026-07-13.
- Netlify env needed: existing `STRIPE_SECRET_KEY`, `APPS_SCRIPT_URL` + new `SFD_API_KEY`, `SFD_SERVER_KEY`, `STRIPE_WEBHOOK_SECRET`, `SFD_RENT_URL`. Stripe dashboard webhook → `rental-webhook`, event `checkout.session.completed`.

## Membership Tiers — Source of Truth (legacy model)

| Tier | Rentals | Loan | Deposit | Access | Visibility |
|---|---|---|---|---|---|
| Matinee | 1 | 7 days | $25 | General | Public |
| Double Feature | 2 | 10 days | $40 | General | Public |
| Premiere | 3 | 14 days | $75 | General + Vault | Invite only, hidden |
| Friends & Family | 2 | 10 days | $0 | General | Invite only, hidden |
| Donor | 3 | 14 days | $0 | General + Vault | Invite only, hidden |

All tiers receive Perks. Apps Script `tierRules` mirrors this table — keep them in sync.

## Member App — Key Architecture Decisions

### Loading and caching
- On startup: member data fetches first, dashboard renders immediately; catalog fetches in background
- **Member cache:** last good member object is stored in localStorage (`'sfd_member_cache_' + token`) and rendered instantly on revisit while the fresh fetch (Apps Script cold start = 1–3s+) completes. If the fetch fails non-auth, the cached dashboard stays up with a stale-data banner. Auth failures clear the cache and show the invalid-link screen.
- **Silent refresh:** `visibilitychange` re-fetches member data when the tab regains focus (60s throttle, `MEMBER_REFRESH_MIN_MS`)
- All backend calls go through `fetchWithTimeout()` — 20s abort (`FETCH_TIMEOUT_MS`). Never use bare `fetch()`.
- Catalog: `catalogCache` (array) + `catalogByIdCache` (keyed) rebuilt after each fetch. `null` = not loaded; `[]` = loaded but empty. Status indicator: amber (loading) → green (ready, fades) → red (failed, tappable retry). Browse screen re-renders on catalog ready/failed too.

### Security in the client
- **`esc()` on every `innerHTML` data interpolation** — all backend and user-typed values must pass through it. Never interpolate IDs into inline `onclick='fn("id")'` strings; use `data-id="..."` + `onclick="fn(this.dataset.id)"`.

### Accessibility conventions
- Div-based controls carry `role="button"` (or `role="checkbox"`) + `tabindex`; a single delegated keydown handler makes Enter/Space trigger click
- Toggle-style buttons (filters, history ranges) update class + `aria-pressed` via `setToggleBtn()`
- Screens receive programmatic focus on navigation (`showScreen`)

### STAR_KEY
- localStorage key for starred items. Initialized as `'sfd_starred_anon'` at parse time, overwritten to `'sfd_starred_' + member.member_id` at top of `renderStatus()` once member loads. Do not initialize with member_id at parse time — member is null then.

### QR Scanner
- jsQR v1.4.0 is **inlined directly in the HTML** (CDN loading fails on Netlify due to CSP) — but at the **end of `<body>`**, before the app script, so it never blocks first paint. `captureQRFrame()` guards with `typeof jsQR === 'undefined'`.
- Tap-to-scan: camera opens, member frames code, taps SCAN, single frame captured. No auto-scan loop.
- `inversionAttempts: 'attemptBoth'` — required for iOS. Canvas downscaled to 640x480 before jsQR.
- Camera failures (denied permission, no mediaDevices) surface a message in `#camFeedback` — never fail silently.

### Checkout / Browse
- Checkout input prefilled `SFD-`; "Keep typing..." hint at 4+ chars; `lookupItem()` fires at 8+; validation via `catalogByIdCache`; final validation server-side
- Browse sort: starred first → available → alpha by `sort_title` (falls back to `title`). Search input is debounced (200ms). Starred IDs are read once per render into a Set — never re-parse localStorage per item.
- Format emoji: 💿 DVD, 📀 Blu-ray, 💽 4K UHD, 🕹️ Video Games (future), 📼 VHS (future)

### Events / Perks / Weekly Rewind feeds
- Three background fetches (`loadEvents`/`loadPerks`/`loadUpdates`, mirroring `loadCatalog`) populate `eventsCache`/`perksCache`/`updatesCache`; `renderFeeds()` fills the `#rewindSection`/`#eventsSection`/`#perksSection` containers on the dashboard (selective update, like `setCatalogStatus` — it does not re-run the full `renderStatus`). Feeds are non-critical: a failed fetch leaves the cache null and the section simply stays hidden.
- Collapsible `.feed-card` accordion (tap headline to expand) shared by all three. Events show a single-tap "I'm going" RSVP and an Add to Google Calendar link; perks show optional photo + text; The Weekly Rewind shows the newest update with a "new" dot (cleared on expand via `REWIND_KEY` localStorage), older active updates listed below.

### Hours & Location
- `lookupMember` returns a `location` object (`address`, `maps_url`, `hours`) from the three `location_*` Settings keys. Hours & Location live **inside the Access Cabinet screen** (no separate tile): `goCode()` renders the cabinet code(s) + do-not-share warning, then `buildLocationHtml()` appends an address card, Get Directions button, and hours table when a location is configured. The cabinet tile subtitle is "Tap to view your code & check current hours". `parseHours()` splits the multi-line `hours` string into day/time rows, sorts Mon→Sun, and highlights the current day. The location section renders nothing when unconfigured. (Members-only app, so the cabinet tile's normal `showCode` gate is fine.)

### Post-Credits Scene (monthly meetup)
- No dedicated code: it's a monthly **Events** row (`rsvp_enabled=TRUE`) so it reuses the Events feed's expand + "I'm going" headcount + Add to Calendar.
- RSVP reuses `member_request` (no backend change) and mirrors the `STAR_KEY` localStorage toggle: `RSVP_KEY = 'sfd_rsvp_' + member.member_id` drives button state; each tap posts `rsvp`/`rsvp_cancel` fire-and-forget.
- The "Tap Check Out or Return Items to get started" hint only renders for members with zero rentals and zero return-pending items.

### Error screen
- "Connection Interrupted" — reassures the member their account is fine; TRY AGAIN reloads. `showError()` uses `innerHTML` so links render (its inputs are hardcoded strings; anything dynamic must be escaped).

### Removed (do not resurrect)
- **Dead-code cleanup 2026-07-17** (~190 lines): the retired Events/Perks feed chain (`loadEvents`, `loadPerks`, `renderEventCard`, `renderPerkCard`, `toggleRsvp`, `buildGCalUrl`, `getRsvped`, `parseEventDate`, `gcalStamp`, `fmtEventWhen`, `eventsCache`/`perksCache`, `RSVP_KEY`) and the member-era fork leftovers (`isOverdue`, `dueDateFromNow`, `daysUntil`, `getToday`, `fmtTs`, `dueLabelStr`, `statCard`, `statCardFull`, the `checkoutItems` queue, `.member-tier`/`.tier-mat`/`.feed-actions`/`.rsvp-on` CSS). Events/perks are posted as Weekly Rewind updates; rent's due dates come from `dueDateStr()`. `submitCustomerLookup()` is the ONE deliberate survivor of the retire-in-place convention (backend `customer_lookup` still live). The vault-code branch in `goCode()` is also deliberately kept (unreachable today — the customer payload has no `vault_access` — but future-proofs a customer vault tier).
- `DEMO_MEMBERS` fallback and demo tokens — gone; no token now shows the not-found screen
- Debug banner — gone
- VLT item prefixes — catalog uses SFD- (sheet-side rename still pending, see queue)

## Version Convention

Every app file shows a date-based version in its footer: `v2026.06.10`. **Bump it to the current date as part of every edit** — there is no build step, so the human (or Claude) doing the edit is the build step. An HTML comment sits above each version line as the reminder. The Apps Script does not need one (Google tracks deployment versions under Manage deployments).

## Known Issues / Active Queue

### In progress
- QR scanner: success feedback (green flash) and SFD-code validation (reject non-SFD QR codes before closing camera)

### Queued
- ~~Pay-per-rental go-live: create Customers/Rentals tabs + Settings keys, set Netlify env + Stripe webhook, deploy backend, test in Stripe test mode~~ — DONE 2026-07-15: extended fee accrual + billing sweep, kept-disc payoff cap, failed-card path, promo codes, receipt emails, and sales tax all tested live end to end (a mix of real live-mode charges and a temporary Stripe test-mode window, reverted after). Sheet cleared of all test data. Remaining from the original scope: **print the cabinet QR** (scifidojo.com/rent) and decide member-app cutover timing — effectively decided as "no cutover," member.html stays dormant unless membership is deliberately rebuilt (see the legacy webhook note below).
- ~~Fix customer_id generation~~ — DONE 2026-07-14: `nextCustomerId_()` mints from the max existing `CUS-###` suffix across **Customers AND Rentals** (a deleted Customers row can still leave its id behind in Rentals history), wrapped in `LockService` so concurrent signups can't race to the same id. Replaces the old `'CUS-' + sheet.getLastRow()` scheme, which reused ids the moment any row above the bottom was deleted (hit live: two CUS-005 accounts, the second inheriting the first's open rental). Didn't need the sheet wiped first — it works against whatever's in the sheet right now, test rows included. Post-launch habit is still worth keeping: mark accounts `status: closed` rather than deleting rows, since a deleted row's id can persist in Rentals/Transactions either way. **Widened to 4 digits 2026-07-15** (`CUS-0001` instead of `CUS-001`), anticipating high volume from one-time customers — the max-suffix regex already matched any digit count, so this was a one-line `padStart` change; existing 3-digit ids keep parsing correctly and coexist with new 4-digit ones, no migration needed.
- Automate the billing sweep (Netlify scheduled function) once the staff-triggered flow is proven.
- Go-live swap from demo Catalog to Catalog Backup. Checklist: set `in_rotation` TRUE on live items (real library is currently ALL FALSE = empty catalog if swapped as-is); clear/process demo transactions first (all 45 demo item_ids collide with real titles — open rentals and history would display wrong movies); unify `sort_title` convention (634 of 759 real items blank)
- VLT prefix → SFD batch rename in sheet
- ~~Enable the `onboard`/`update_stripe` key gate~~ — DONE 2026-07-14, gated on `SERVER_KEY` (stronger choice than the originally-planned `api_key`, since `API_KEY` is public in page source). **Blocking follow-up:** confirm `onboard-member.js` and `stripe-webhook.js` in the onboarding repo send `server_key: process.env.SFD_SERVER_KEY` — a prior round of hardening added `api_key` to those two functions instead, which will now fail this gate and break member signup/Stripe activation until fixed. No new env var needed, `SFD_SERVER_KEY` already exists.
- Stripe Customer Portal for self-service plan changes
- ~~Self-service card update~~ — DONE and verified live 2026-07-15: tested the full recovery loop (signup with a card that fails, sweep flags `payment_status: failed` and blocks renting, UPDATE CARD swaps to a working card, sheet flips back to `ok`, rentals unblock), not just a working-card-for-working-card swap. Owed charges are NOT auto-retried on card update — the next billing sweep (or CHARGE & CLOSE at return) collects, confirmed by design, unchanged.
- ~~"Send me my account link" recovery~~ — DONE and deployed 2026-07-13: signup requires both email + phone, emails the link at signup, and the `send_link` action + EMAIL ME MY LINK button cover lost bookmarks. Verified live end to end (welcome email, recovery email, SFD Mailer relay).
- ~~Maximum-charge copy is ambiguous about the base price~~ — DONE 2026-07-17 as part of the pre-launch copy pass: the cap is now framed everywhere as the all-in total ("The most you will pay in total, base price and fees combined, is $X plus tax") — item-confirm card, Browse's detail fact ("Maximum total charge $X plus tax, base price included"), the success receipt, the receipt view, terms.html (cheat sheet + full version), and the Stripe Checkout line-item description in `start-rental.js`. The same pass added "plus tax" to every customer-facing price quote (see the Billing policy sales-tax bullet) and reworded the remaining "at the counter"/"ask staff" phrasings in terms.html and the backend's return-cooldown message to email-based wording, per the unstaffed-store copy rule.

### Deferred
- Vault visual treatment in Browse (no vault titles in collection yet — real library currently has zero `collection=vault` items)
- Sort by format in Browse; public inventory browser. (Monthly meetup "Post-Credits Scene" is handled via an Events row, not separate tooling.)
- **Legacy member webhook is broken, not fixed** (found 2026-07-15): the onboarding repo's `stripe-webhook.js` (legacy membership signups, distinct from `rental-webhook.js`) and `rental-webhook.js` were both apparently configured to read the same Netlify env var, `STRIPE_WEBHOOK_SECRET` — but each is a separate Stripe endpoint with its own unique signing secret, so only one of the two can ever verify correctly at a time. Since this session's testing confirmed `STRIPE_WEBHOOK_SECRET` holds the rental endpoint's secret, the legacy member webhook has likely been silently failing signature verification since the rental system went live. **Not fixed, deliberately**: member.html/the deposit-based membership flow is fully dormant (replaced by pay-per-rental) and would need a rebuild from scratch if ever revived, so patching this webhook in isolation isn't worth it. If membership is ever rebuilt: give `stripe-webhook.js` its own distinct env var (e.g. `STRIPE_MEMBER_WEBHOOK_SECRET`) instead of sharing `STRIPE_WEBHOOK_SECRET` with the rental webhook.

## Working Conventions

### Two-session split (this repo vs. the onboarding repo)
Two Claude Code sessions work on this project in parallel: this one (scoped to the public `sci-fi-dojo` repo) and a sister session scoped to the private onboarding repo (`scifidojo-onboarding`, Netlify functions + `sfd-staff.html` + `sfd-onboarding.html` + `success.html`). Neither session has git access to the other's repo. The Apps Script backend is a third thing again — not a git repo at all, just pasted into the Apps Script editor — and both sessions can touch it conceptually, so a firm split is needed (decision 2026-07-14):
- **All Apps Script (`.gs`) changes flow through this session**, regardless of which session first spots the need. This session keeps the canonical working copy (in its scratchpad) and is responsible for testing before handing a deploy-ready file back to the user.
- **All onboarding-repo changes (Netlify functions, `sfd-staff.html`, etc.) get handed off to the sister session** rather than delivered here for the user to commit directly — that session actually has repo access and can commit/test in place.
- Since neither session can message the other directly, the user is the courier: paste this session's Apps Script deliverables into the onboarding session when it needs backend context, and paste that session's onboarding-repo work back here when this session needs to reconcile against it (e.g. the 2026-07-14 `checkStaffPin_`/`onboard` hardening, discovered this way and merged in).
- Consequence already hit once: the two sides drifted (Apps Script gated `onboard`/`update_stripe` on `SERVER_KEY`, but the onboarding-repo functions sent no key at all) because each session built its half without seeing the other's state. Prefer over-sharing context across sessions to re-litigating whether a fix already landed.

### Dyslexia-friendly code changes
- All code changes use explicit FIND/REPLACE with file name and context
- Edits must be surgical — no layout, CSS, or structural changes unless explicitly requested
- No broad rewrites unless asked. Always specify the exact file being edited.

### Copy / tone (app and public-facing prose)
- Direct, confident, slightly cinematic — never corporate, never quirky
- No em-dashes or emojis in prose
- Location references use "Belmar, NJ" (confirmed 2026-07-08)
- Invite-only tiers hidden from all public-facing copy
- Collection should feel welcoming to all film fans, not genre-restricted
- Perks (snacks, surprises) are cheap PAID items via an honor box (decision 2026-07-08) — copy should not promise free stuff; trade-ins are rewarded in free rentals (exact math TBD)

### CSS Variables (member app palette)
```css
:root {
  --bg:       #080c12;
  --panel:    #0d1520;
  --panel2:   #111c2a;
  --border:   #1e3450;
  --border2:  #2a4868;
  --accent:   #00d4ff;
  --accent2:  #ff6b35;
  --success:  #00ff9d;
  --warning:  #ffcc00;
  --danger:   #ff3b5c;
  --vault:    #c084fc;
  --text:     #e8f4ff;
  --text-mid: #a8c8e8;
  --text-dim: #6a90b0;
  --text-lbl: #8ab4d4;
  --glow:     0 0 20px rgba(0,212,255,.25);
  --r:        14px;
  --r-sm:     9px;
}
```

## Contact / Accounts
- Email: scifidojo@aol.com · Instagram: @scifi_dojo
- GitHub repo `sci-fi-dojo` (public, this one) + private onboarding repo → both auto-deploy via Netlify
- Google account manages Sheets + Apps Script; Stripe is the payment platform
